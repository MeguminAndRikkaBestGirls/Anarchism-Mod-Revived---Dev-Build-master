check_has_focus_tree_to_switch_to_monarchism = {
	NOT = { tag = GER }
	NOT = { tag = JAP }
	NOT = { tag = HUN }
	NOT = { tag = ROM }
	NOT = { tag = FRA }
}

check_has_focus_tree_to_switch_to_libsoc = {
	NOT = { tag = PRC }
	NOT = { tag = FRA }
	NOT = { tag = CAN }
	NOT = { tag = AST }
	NOT = { tag = NZL }
	NOT = { tag = SAF }
}